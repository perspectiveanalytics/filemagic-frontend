# Local dev can use the Astro proxy at /api.
# Production should use https://api.filemagic.app/api unless the frontend
# container intentionally exposes an allowed /api proxy.
PUBLIC_API_BASE_URL=/api
PUBLIC_TURNSTILE_SITE_KEY=
